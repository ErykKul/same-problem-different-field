# Reproduction package dependencies.
#
# Core (corpus build + reproduce: TF-IDF, metrics, fetch/convert):
numpy>=1.24
scikit-learn>=1.3
requests>=2.28
PyMuPDF>=1.23
beautifulsoup4>=4.12
lxml>=4.9
markdownify>=0.11
#
# Heavier ML extras (the SOTA scientific / general embedders compared against):
# SPECTER, SPECTER2, SciNCL, SemCSE, Qwen3-Embedding, E5-large-v2. Drop these if you only
# want the TF-IDF rows; reproduce.py skips any embedder it cannot load and still prints the
# rest of the table. (adapters is needed only for the SPECTER2 baseline, src/specter2_local.py.)
sentence-transformers>=2.5
transformers>=4.40
torch>=2.1
adapters>=1.0
