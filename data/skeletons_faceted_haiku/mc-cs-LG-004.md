MECHANISM: Calibrate a nonparametric prediction interval method on a validation set under an assumption of exchangeability. Evaluate coverage of prediction sets on held-out test data experiencing a known temporal distribution shift. Quantify degradation of coverage guarantees as a function of feature stability (measured via Jaccard overlap) and feature importance concentration. Test retraining at multiple frequencies to determine if periodic model updates restore guarantees. Use SHAP importance analysis to diagnose failure modes.
DOMAIN: Conformal prediction under distribution shift with temporal splits
STRUCTURE: other: importance sampling and adaptive calibration
DATA_OBJECT: sequence or time-series
INFERENCE: sampling or Monte-Carlo
PROBLEM_FORM: prediction or classification
DISTRIBUTION: binary; binary
COMPLEXITY: convergence rate
