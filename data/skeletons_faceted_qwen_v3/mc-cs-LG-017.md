MECHANISM: The paper computes a binary classification model to distinguish outputs from a target generative model from other sources. It begins by extracting high-dimensional feature vectors from input entities using a pre-trained encoder. These features are then used to train a linear classifier with a binary cross-entropy loss function, where the goal is to assign a probability that an entity originates from the target model. To improve generalization, the model is fine-tuned using unlabeled data from diverse sources, which may include real entities, outputs from unknown generators, or even samples from the target model itself. The fine-tuning process involves a constrained optimization objective that minimizes the loss on the unlabeled data while enforcing a constraint that the loss on the labeled data does not exceed a predefined threshold. This constraint ensures that performance on known sources remains stable while allowing the model to adapt to the diversity of the unlabeled data. The optimization is implemented as a Lagrangian relaxation, combining the two loss terms with a regularization parameter that balances the trade-off between improving generalization and preserving accuracy on labeled data. The final model outputs a probability score indicating the likelihood that an entity was generated by the target model, with a threshold applied to make a binary decision. The method explicitly avoids assuming any specific distributional form for the input data and does not require explicit labels for the unlabeled data, instead treating them as a signal to expand the decision boundary. The algorithm is iterative and relies on gradient-based optimization to adjust the classifier parameters while maintaining the constraint on labeled data performance. The approach is designed to be robust in open-world settings where new generators may emerge and where labeled data is limited.  
DOMAIN: machine learning for image attribution  
STRUCTURE: dense linear algebra  
DATA_OBJECT: dense matrix or tensor  
INFERENCE: deterministic or closed-form  
PROBLEM_FORM: classification  
DISTRIBUTION: none  
COMPLEXITY: not stated  
DATA_AVAILABILITY: none  
CODE_AVAILABILITY: none  
PREREGISTRATION: none  
EVIDENCE_BASIS: empirical-with-released-data
